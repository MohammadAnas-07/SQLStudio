GEMINI_API_KEY=
DATABASE_URL="file:./metadata.db"
PORT=3000
JWT_SECRET=
# Optional. Defaults to ~/Desktop/sql-workspace; Docker overrides this to /app/workspace
WORKSPACE_ROOT_PATH=
# Optional. Defaults to http://localhost:5173. Never '*' if deployed publicly.
CORS_ORIGIN=
