# Optional. Base URL of the backend API. Defaults to http://localhost:3000
# for local dev. Set this to your deployed backend's URL (e.g.
# https://api.your-domain.com) whenever the frontend and backend aren't
# both running on localhost:3000 — any real deployment.
VITE_API_BASE_URL=
